{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug reproductor",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/reproductor.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "miDebuggerPath": "gdb.exe",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "build reproductor"
        }
    ]
}
